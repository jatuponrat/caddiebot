{
  "name": "caddiebot-engine",
  "version": "1.0.0",
  "description": "Golf caddie game engine + LINE webhook. Deterministic backend logic for handicap, level classification, net scores, and Thai/English message parsing.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env --watch server.js",
    "start:local": "node --env-file=.env server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
