# คู่มือการใช้งาน Caddiebot ⛳

บอทแคดดี้สำหรับก๊วนกอล์ฟใน LINE — สร้างเกม, คิดแต้มต่อ, เดินเกมทีละหลุม
และคิดเงินแบบเทียบเป็นคู่ให้อัตโนมัติ ทุกคนพิมพ์คุยกันในกลุ่ม LINE ได้เลย

> บัญชี: **SwingHouse Family** (`@swinghouse`) · ทำงานบน `https://caddiebot.onrender.com`

---

## 1. เริ่มต้น

1. เพิ่มบอทเป็นเพื่อน (ID `@swinghouse`) แล้ว **เชิญเข้ากลุ่ม** ของก๊วน
2. บอททักทายและบอกคำสั่งให้อัตโนมัติ
3. แต่ละกลุ่ม = หนึ่งเกม (ไม่ปนกัน) และเกมอยู่ได้ **12 ชั่วโมงนับจากการเล่นครั้งล่าสุด**
   (นับใหม่ทุกครั้ง — ลงสกอร์/ตอบคำถามตั้งค่า/สรุปเงิน ต่ออายุให้อีก 12 ชม.)

> ⏱️ **กฎ 12 ชั่วโมง (แบบนับใหม่)** — ตราบใดที่ยังมีคนลงสกอร์ เกมไม่ตายกลางรอบเด็ดขาด
> เกมจะถูกล้างทิ้งเมื่อ **เงียบครบ 12 ชม.** (ผู้เล่น สนาม เดิมพัน สกอร์ หายทั้งหมด)
> หลังจากนั้น **บอทจะเงียบสนิท ไม่ตอบอะไรเลย** จนกว่าจะมีคนพิมพ์ `สร้างเกม` หรือ `สร้างเกมส์`
> ถึงจะเริ่มเกมใหม่ตั้งแต่ต้น

---

## 2. ขั้นตอนเล่นทั้งเกม

**1) สร้างเกม** — พิมพ์ `สร้างเกม 4 คน` แล้วบอทจะถามทีละขั้น:

- **ชื่อสนาม** — พิมพ์ `The Pine` / `Rachakram` (หรือสนามที่เคยบันทึก) → โหลดพาร์อัตโนมัติ;
  เพิ่มสนามใหม่ด้วย `ชื่อ + พาร์ 18 หลุม` เช่น `Kbsc 454435434 435444354` → บอทคิดพาร์รวมให้
  ยืนยัน (พิมพ์ `ยืนยัน`) แล้วบันทึกไว้ใช้ครั้งหน้าได้เลย
- **เล่นหลุมละเท่าไร** — พิมพ์ตัวเลข เช่น `20`
- **มีเทอร์โบไหม** — `มี` / `ไม่มี` (ถ้ามี เทอร์โบลงหลุม 9 และ 18 เดิมพัน ×2)

**2) เข้าร่วม** — แต่ละคนพิมพ์ `เข้าร่วม [ชื่อ] [สกอร์ 3 รอบล่าสุด]`
เช่น `เข้าร่วม แซม 105 90 91` — บอทคิดแต้มต่อให้ทันที

**3) เริ่มเล่น** — พอครบจำนวนคน บอทขึ้น `🏁 ผู้เล่นครบแล้ว! ⛳ หลุม 1 Par 4 เริ่ม !!`

**4) ลงแต้มแต่ละหลุม** — ส่งทีละคน `หลุม 1 แซม 5` (ย่อ `H1 แซม 5`) หรือรวม `หลุม 1 เอ 4 บี 5`
พอครบทุกคน บอทคิดเงินหลุมนั้นแล้วขึ้นหลุมถัดไปให้เอง ไล่จนจบหลุม 18
(หรือส่งรวดเดียวทั้ง 18 หลุม: `แซม 544535445 445354454` — แบบนี้ใส่ได้หลุมละ 1–9 เท่านั้น)

> ⛳ **แต้มต้องเป็นตัวเลขเสมอ 1–10** ไม่มีคำสั่งยอมแพ้
> ตีเกิน 10 ให้ลงเป็น **10** — ถ้าพิมพ์ 12 มา บอทจะปรับให้เป็น 10 แล้วบอกในข้อความว่า
> `⚠️ ปรับเป็นสูงสุด 10: แซม 12→10` จะได้เห็นชัดว่าถูกปรับ ไม่ใช่แอบเปลี่ยนเงียบๆ
> หลุมไหนยังไม่ได้ใส่เลข = ยังไม่ถือว่าลงสกอร์ บอทจะรอคนนั้นอยู่

**5) เช็คยอดระหว่างเล่น** — พิมพ์ `ยอดล่าสุด` (หรือ `ยอดตอนนี้` / `ใครนำ`)
บอทเรียงอันดับให้ พร้อมบอกว่าเหลืออีกกี่หลุมและเทอร์โบเหลือหลุมไหน

> 🔄 **พอจบหลุม 9 บอทสรุปให้เองอัตโนมัติ** ไม่ต้องพิมพ์ถาม — ทั้งยอดเงินสะสม
> **สกอร์ OUT ของทุกคน** และคำถามว่าจะเปลี่ยนแต้มต่อสำหรับ 9 หลุมหลังไหม
> เปลี่ยนหลุมที่สรุปได้ที่ตัวแปร `TURN_HOLES` ใน `session.js`

```
📋 สกอร์ OUT (หลุม 1-9 · พาร์ 36)
เรียว  5 5 4 6 4 8 4 6 6 = 48 (+12)
หนึ่ง  5 5 4 7 6 5 7 5 5 = 49 (+13)
แซม    6 8 4 7 6 5 4 5 5 = 50 (+14)
ติน    5 5 8 8 6 7 5 7 4 = 55 (+19)
หมวย   6 5 5 9 8 9 3 7 8 = 60 (+24)
```

### แต้มต่อใหม่สำหรับ 9 หลุมหลัง

พอจบหลุม 9 บอทจะถามเอง:

```
🔁 9 หลุมหลังจะใช้แต้มต่อใหม่ไหมครับ?
(คิดจากสกอร์ 9 หลุมแรก ×2 — แซม 97 → 100 · เรียว 94 → 96 · หมวย 112 → 120 · ...)
พิมพ์ "แต้มต่อใหม่" เพื่อเปลี่ยน · "แต้มต่อเดิม" เพื่อใช้ของเดิม
```

- **แต้มต่อใหม่ = สกอร์ 9 หลุมแรก × 2** — ใครฟอร์มดีวันนี้แต้มต่อจะลดลงทันที
- ⚠️ **หลุม 1-9 คิดเงินด้วยแต้มต่อเดิมเสมอ ไม่เปลี่ยนย้อนหลัง** บอทแช่ตารางเก่าไว้
  ให้ก่อน แล้วใช้ตารางใหม่เฉพาะหลุม 10-18
- เปลี่ยนได้ครั้งเดียวต่อเกม และต้องลงสกอร์ 9 หลุมแรกครบทุกคนก่อน

```
💰 ยอดล่าสุด · ห้อง 2361 (เล่นไปแล้ว 7/18 หลุม)

1. ติน    +180
2. เรียว  +60
3. แซม    −60
4. หนึ่ง  −80
5. หมวย   −100

เหลืออีก 11 หลุม · หลุมเทอร์โบที่ยังไม่เล่น: 9, 18 🔥
```

**6) สรุปเงิน** — `รวม 18` (สรุปเต็ม) · `จบเกม` (สรุปแล้วปิดห้อง)

---

## 2.5 เลขห้อง และประวัติย้อนหลัง

### เลขห้องบอกวันที่

เลขห้องคือ **วันเดือนปี (ววดดปป) + เลขรันของวันนั้น**

```
160826001
└─┬──┘└┬┘
  │    └── รอบที่ 1 ของวันนั้น
  └─────── 16 ส.ค. 26
```

ใช้ **เวลาไทย (UTC+7)** เสมอ ไม่ใช่เวลาเซิร์ฟเวอร์ (Render รันเป็น UTC) —
ออกรอบตี 6 ก็ยังได้วันที่ถูก วันเปลี่ยนตอนเที่ยงคืนไทยพอดี
และเลขไม่ซ้ำข้ามวันอีกต่อไป จึงค้นประวัติด้วยเลขห้องได้จริง

> เช็คหลัง deploy: เปิด `/health` ดูค่า `thai_time_now` กับ `today_room_prefix`
> ต้องตรงกับนาฬิกาบนมือคุณ ไม่ใช่เวลา UTC

### ดูประวัติ

| พิมพ์ | ได้อะไร |
|---|---|
| `ประวัติ` | 5 รอบล่าสุดของกลุ่มนี้ พร้อมเลขห้อง |
| `ประวัติ 160826001` | รายละเอียดรอบนั้น — สนาม แต้มต่อ เงิน สกอร์รวม |

พิมพ์แยกขีดก็ได้ (`ประวัติ 160826-001`) และเลขห้องเก่าแบบ 4 หลักก็ยังค้นได้

> `ประวัติ` เป็นคำสั่งเดียวที่ตอบได้แม้บอทจะเงียบอยู่ (ไม่มีเกมค้างในกลุ่ม) —
> เพราะเวลาที่คนอยากดูประวัติ คือตอนที่เกมจบไปแล้ว

### รอบไหนบ้างที่ถูกเก็บ

ทุกรอบที่ลงสกอร์อย่างน้อย 1 หลุม จะถูกบันทึกลงฐานข้อมูล **ก่อน** ถูกลบเสมอ

| จบแบบไหน | สถานะที่แสดง |
|---|---|
| พิมพ์ `จบเกม` | จบเกม |
| พิมพ์ `รวม 18` แล้วเลิกกัน | รวมเงินแล้ว |
| เงียบครบ 12 ชม. | หมดเวลา 12 ชม. |
| พิมพ์ `สร้างเกม` ทับรอบเดิม | ถูกสร้างเกมใหม่ทับ |

ห้องที่ตั้งไว้แต่ยังไม่ได้ลงสกอร์เลย จะไม่ถูกเก็บ (ไม่มีอะไรให้ดู)

### ดูจากเบราว์เซอร์

```
https://caddiebot.onrender.com/rounds/160826001         → รอบนั้น (JSON)
https://caddiebot.onrender.com/rounds?source=<groupId>  → รอบล่าสุดของกลุ่ม
```

---

## 3. แต้มต่อ (Handicap)

ใช้ 3 รอบล่าสุด ถ่วงน้ำหนักรอบดีที่สุด (ต่ำสุด) ×2:
`แต้มต่อ = ปัดเศษ( (รอบดีที่สุด×2 + อีกสองรอบ) ÷ 4 )`
ตัวอย่าง `92,95,90` → (90×2+92+95)/4 = **92**

**การต่อสโตรคคิดเป็นรายคู่** — แต่ละคู่ดูส่วนต่างแต้มต่อของคู่นั้นเอง
ไม่ใช่เหมาทั้งวงจากส่วนต่างคนเก่งสุด–อ่อนสุด

| ส่วนต่างของคู่นั้น | ระดับ | พาร์ 3 | พาร์ 4 | พาร์ 5 | รวมทั้งรอบ (พาร์ 72) |
|---|---|---|---|---|---|
| น้อยกว่า 3 | 0 | 0 | 0 | 0 | **0** |
| 3–6 | 1 | 0 | 0 | 1 | **4** |
| 7–12 | 2 | 0 | 1 | 0 | **10** |
| 13–16 | 3 | 0 | 1 | 1 | **14** |
| 17–20 | 4 | 1 | 1 | 1 | **18** |
| 21–24 | 5 | 1 | 1 | 2 | **22** |
| 25–30 | 6 | 1 | 2 | 1 | **28** |
| มากกว่า 30 | 7 | 1 | 2 | 2 | **32** |

ในแต่ละคู่ คนแต้มต่อต่ำกว่าเป็นผู้ให้ อีกคนรับตามตาราง · แก้ตารางได้ที่ `engine.js`

ตัวอย่าง วง 5 คน — หนึ่ง 92, เรียว 94, แซม 97, หมวย 112, ติน 119:

- หนึ่ง–เรียว ห่าง 2 → **ไม่ต่อกัน**
- หนึ่ง–แซม ห่าง 5 → แซม**รับ 1 แต้มเฉพาะพาร์ 5** (รวม 4)
- หมวย–ติน ห่าง 7 → ติน**รับ 1 แต้มเฉพาะพาร์ 4** (รวม 10)
- หนึ่ง–ติน ห่าง 27 → ติน**รับ 1 / 2 / 1** (รวม 28)

> ⚠️ ก่อนหน้านี้บอทเหมาให้ทุกคนที่ไม่ใช่คนเก่งสุด**รับเท่ากันหมด** ทำให้คนที่ห่างคนเก่งสุดแค่
> 2–5 แต้มได้แต้มต่อเต็มจำนวนโดยไม่ควรได้ ยอดเงินจึงเพี้ยนหนัก แก้แล้วตั้งแต่รอบนี้

**ตารางซอย 8 ระดับเพื่อลดการกระโดดที่ขอบขั้น** — ของเดิมมี 6 ระดับ และช่วงรอยต่อ
กระโดดทีละ 10 แต้ม (ห่าง 5 ได้ 0 แต่ห่าง 6 ได้ 10) ตอนนี้กระโดดมากสุด 6 แต้ม

**สกอร์เน็ตที่บอทแสดง** คิดเทียบกับคนเก่งสุดในวง ส่วนการคิดเงินใช้แต้มต่อของคู่นั้นๆ
ทั้งสองเลขจึงอาจไม่ตรงกันเป๊ะเมื่อเทียบข้ามคู่ — ยึด "สรุปเงิน" เป็นหลัก

### เช็คแต้มต่อก่อนออกรอบ

พิมพ์ **`แต้มต่อ`** (หรือ `ใครต่อใคร` / `hcp`) บอทจะพิมพ์ตารางว่าคู่ไหนต่อกันเท่าไร
ใช้ตกลงกันในก๊วนก่อนเริ่มเล่น จะได้ไม่ต้องมาเถียงกันตอนเคลียร์เงิน

```
⛳ ตารางแต้มต่อ · ห้อง 7876

แต้มต่อ: หนึ่ง 92 · เรียว 94 · แซม 97 · หมวย 112 · ติน 119

📥 ติน รับแต้มต่อ
• จาก หนึ่ง (ห่าง 27) → พาร์3:1 พาร์4:2 พาร์5:1 = 28 แต้ม
• จาก หมวย (ห่าง 7) → พาร์3:0 พาร์4:1 พาร์5:0 = 10 แต้ม

📥 แซม รับแต้มต่อ
• จาก หนึ่ง (ห่าง 5) → พาร์3:0 พาร์4:0 พาร์5:1 = 4 แต้ม
```

ท้ายตารางบอทจะถามว่าตกลงไหม:

- พิมพ์ **`ตกลง`** → บอทพากลับไปที่หลุมที่กำลังรอสกอร์อยู่ เล่นต่อได้เลย
- พิมพ์ **`เข้าร่วม [ชื่อเดิม] [3 สกอร์ใหม่]`** → แก้สกอร์ แล้วเรียก `แต้มต่อ` ซ้ำเพื่อตรวจ

เรียกดูตอนไหนก็ได้ ไม่กระทบเกมที่เล่นอยู่ — เรียกกลางรอบแล้วตอบ `ตกลง`
บอทจะกลับไปที่**หลุมปัจจุบัน** ไม่ใช่หลุม 1 สกอร์ที่ลงไปแล้วไม่หาย

---

## 4. วิธีคิดเงิน (เทียบเป็นคู่)

แต่ละหลุม เทียบ **net** (gross − สโตรคที่ได้) ของผู้เล่นทุกคู่:
คู่ไหน net ต่ำกว่าได้เงินหลุมละ [เดิมพัน] จากคู่ที่แพ้ · เสมอ = ไม่จ่าย ·
หลุมเทอร์โบ (9, 18) เดิมพัน ×2 · ยอดรวม = ผลบวกทุกหลุม (บวก = ได้ / ลบ = จ่าย)

> บอททำหน้าที่ "นับเกม" และเตรียมข้อมูลให้ระบบหลังบ้าน ไม่ได้โอนเงินจริง

---

## 5. สนามสำเร็จรูป

- **The Pine** — พาร์ `445345434 453443544` (รวม 72)
- **Rachakram** — พาร์ `445344435 435454434` (รวม 72)

พิมพ์ชื่อสนามตอนตั้งค่าเพื่อโหลดพาร์อัตโนมัติ (เพิ่มสนามใหม่ได้ใน `parser.js`)

---

## 6. ข้อจำกัด

- **ต้องตั้ง `DATABASE_URL` (Postgres/Supabase) บน Render** ไม่ใช่ตัวเลือก:
  ถ้าไม่ตั้ง เกมจะอยู่ในหน่วยความจำเท่านั้น และ Render ฟรีหลับทุก ~15 นาทีที่ว่าง
  → เกมหายทุกครั้งที่เว้นช่วงระหว่างหลุม (อาการ "บอทเงียบ เล่นไม่ได้")
- ตั้ง `DATABASE_URL` แล้ว: สนาม ประวัติสกอร์ **และเกมที่กำลังเล่น** ถูกเก็บลง DB
  รีสตาร์ต/หลับแล้วตื่น บอทจะดึงเกมของกลุ่มนั้นกลับมาให้เอง
- ตั้ง `SELF_URL=https://caddiebot.onrender.com` เพื่อให้บอท ping ตัวเองทุก 10 นาที ไม่ให้หลับ
- เงียบครบ 12 ชม. บอทจะไม่ตอบข้อความใด ๆ จนกว่าจะพิมพ์ `สร้างเกม` ใหม่
  (ยกเว้น `ประวัติ` ที่ตอบได้เสมอ) — รอบที่ตายจะถูกเก็บลงประวัติก่อนถูกลบ
- **บอทรับเฉพาะข้อความ** — รูปภาพ สติกเกอร์ ไฟล์ บอทจะเงียบ ไม่ตอบอะไรเลย
  กรอกพาร์สนามด้วยการพิมพ์แทน (ชื่อสนามสำเร็จรูป หรือเลขพาร์ 18 หลัก)
- ส่งสกอรวดเดียวรองรับหลุมที่ตี 1–9; หลุมที่ตี 10+ ใช้ `หลุม X …` แทน

---

📄 ดูชุดข้อความตอบทั้งหมดของบอทได้ที่ **CADDIEBOT_WORDING.th.md**
